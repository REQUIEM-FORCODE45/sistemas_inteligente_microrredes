services:
  redis:
    image: redis:7-alpine
    container_name: microrred-redis
    command: redis-server --protected-mode no
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  redis_data:
